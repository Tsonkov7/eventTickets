/*
    Middlewares are functions that run before the request is handled by the route handler.
    They can be used to validate the request, parse the request body, etc.
    Example:
    - validate the request body
    - parse the request body
    - validate the request params
    - validate the request query
    - validate the request headers
    - validate the request cookies
    - validate the request session
    - validate the request authentication
    - validate the request authorization
    - validate the request rate limiting
    - validate the request cors
    - validate the request compression
    - validate the request logging
    - validate the request error handling
    - validate the request response
*/
