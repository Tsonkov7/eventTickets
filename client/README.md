# RavePass Client

React + TypeScript frontend for the RavePass event ticketing app.

See the [root README](../README.md) for full setup, environment variables, and deployment instructions.

```bash
npm install
cp .env.example .env   # set VITE_API_BASE_URL and VITE_STRIPE_PUBLISHABLE_KEY
npm run dev
```
